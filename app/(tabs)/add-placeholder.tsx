import { View } from 'react-native';

// This is a placeholder screen for the add button tab
// The actual import modal is triggered via router.push from the tab button
export default function AddPlaceholder() {
  return <View />;
}
